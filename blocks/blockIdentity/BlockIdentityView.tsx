import { FC } from 'react'
import { BlockIdentityComp } from './BlockIdentityComp'
import { BlockViewProps } from '../../../../../utils/typescript-utils'

const BlockIdentityView: FC<BlockViewProps> = ({ theme }) => <BlockIdentityComp theme={theme} />

export default BlockIdentityView
